function tahminiKontrolEt(targetPage) {
    if (targetPage) {
        window.location.href = targetPage; // Hedef sayfaya yönlendir
    } else {
        alert("Hedef sayfa belirtilmemiş!");
    }
}
